using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Winda : MonoBehaviour {



  //  // Update is called once per frame
   // void Update()
    //{
     //   float distCovered = (Time.time - 0) * 2;
      //  float fracJourney = distCovered / 2;
       // transform.position = Vector3.Lerp( ( 0 , 0 ,0), 5 , 3);
   // }
}
